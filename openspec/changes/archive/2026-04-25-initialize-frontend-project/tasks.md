## 1. 工程脚手架

- [x] 1.1 确认仓库根目录没有会与 Next.js 初始化冲突的既有前端文件。
- [x] 1.2 初始化 Next.js 项目，并启用 TypeScript、App Router 和 `src/` 目录结构。
- [x] 1.3 确认 `package.json` 包含开发、构建、lint 和类型检查所需脚本。
- [x] 1.4 提交或保留依赖锁文件，确保后续安装结果可复现。

## 2. 样式与组件系统

- [x] 2.1 安装并配置 Tailwind CSS，确认全局样式入口被 Next.js 根布局引用。
- [x] 2.2 初始化 shadcn/ui，生成 `components.json` 并配置组件、工具函数和样式路径。
- [x] 2.3 创建 `src/lib/utils.ts`，提供 shadcn/ui 组件所需的 className 合并函数。
- [x] 2.4 确认 Tailwind 配置会扫描 `src/app`、`src/components` 和相关源码目录。

## 3. 基础目录与最小页面

- [x] 3.1 创建基础目录：`src/app`、`src/components`、`src/components/ui`、`src/lib`、`src/types` 和 `src/styles`。
- [x] 3.2 创建最小根布局，设置基础 metadata、语言和全局样式引用。
- [x] 3.3 创建最小首页，用于验证项目可渲染，不包含世界沙盘业务流程。
- [x] 3.4 确认源码路径别名可用于引用组件和工具函数。

## 4. 验证

- [x] 4.1 安装依赖并确认安装过程无错误。
- [x] 4.2 运行 lint 或静态检查，确认工程配置无错误。
- [x] 4.3 运行 TypeScript 类型检查或生产构建，确认类型配置可用。
- [x] 4.4 启动本地开发服务器并访问根路径，确认页面正常渲染。

## 5. 收尾与回滚确认

- [x] 5.1 检查新增文件范围，确认未引入后端、数据库、Agent 或业务流程实现。
- [x] 5.2 记录实际使用的包管理器、关键命令和本地运行地址。
- [x] 5.3 确认回滚方式：删除新增前端工程文件和源码目录，或 revert 本次初始化提交。
