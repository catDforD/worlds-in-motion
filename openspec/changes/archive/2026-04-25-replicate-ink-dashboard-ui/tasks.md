## 1. 视觉资源与数据准备

- [x] 1.1 梳理参考图中的页面模块、文案、数字、角色、事件和状态信息，形成静态 mock 数据结构。
- [x] 1.2 确认可用的本地视觉资源，包括山水横幅、章节插图、角色头像和纹理背景；缺失资源用可替换占位资产或生成资产补齐。
- [x] 1.3 定义 TypeScript 类型，覆盖导航项、统计项、事件、章节、角色、关系、秘密和趋势数据。
- [x] 1.4 将 mock 数据集中放入页面附近或 `src/lib`，避免在展示组件中硬编码复杂数据。

## 2. 页面结构与组件拆分

- [x] 2.1 将根路径最小首页替换为织世录工作台页面入口。
- [x] 2.2 实现左侧深色导航栏，包含品牌、导航项、active 状态、底部执笔者信息和工具按钮。
- [x] 2.3 实现顶部世界横幅，包含“烟雨江南”标题、编辑图标、描述、标签和右上操作按钮。
- [x] 2.4 实现统计概览和运行状态模块，显示五类统计项、运行状态、进度条和暂停操作。
- [x] 2.5 实现近期事件、最新故事章节和活跃角色模块，保持参考图中的信息层级和卡片密度。
- [x] 2.6 实现右侧信息栏，包含当前时间、紧张关系、待解秘密和近期趋势模块。

## 3. 水墨古风视觉实现

- [x] 3.1 在全局样式或页面样式中建立宣纸、墨色、松绿、朱砂、灰青和边框等主题变量。
- [x] 3.2 实现页面宣纸背景、侧栏墨色质感、细线卡片边框、轻阴影和山水装饰层。
- [x] 3.3 使用 lucide-react 图标补齐导航、统计、操作和趋势符号，并为图标按钮提供可访问名称。
- [x] 3.4 调整中文排版、字号层级、卡片间距、圆角和按钮样式，使首屏比例接近参考图。
- [x] 3.5 确保头像、插图、进度条、关系条和优先级标记在视觉上稳定且不拉伸失真。

## 4. 响应式与可用性

- [x] 4.1 为桌面宽屏实现三栏布局，确保首屏主要模块完整可见。
- [x] 4.2 为中小屏实现可滚动降级布局，侧栏、主内容和右侧信息栏均可访问。
- [x] 4.3 为事件、章节摘要、角色目标和关系条目处理长文本换行、截断或限定高度。
- [x] 4.4 检查按钮、标签、头像和固定格式 UI 元素的尺寸稳定性，避免 hover 或动态文本导致布局跳动。

## 5. 验证与收尾

- [x] 5.1 运行 `npm run lint`，修复 lint 错误。
- [x] 5.2 运行 `npm run typecheck` 或 `npm run build`，确认 TypeScript 和构建链路可用。
- [ ] 5.3 启动本地开发服务器，使用浏览器访问根路径并截取桌面宽屏截图。
- [ ] 5.4 使用浏览器窄屏视口检查响应式降级，确认无明显重叠、溢出或空白视觉资源。
- [x] 5.5 检查改动范围，确认未接入后端、数据库、Agent 或真实业务流程。
