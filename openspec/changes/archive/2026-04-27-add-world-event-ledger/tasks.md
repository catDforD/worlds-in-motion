## 1. 类型与本地运行态模块

- [x] 1.1 定义 `WorldRuntimeState`、运行事件、章节草稿和最新章节 TypeScript 类型
- [x] 1.2 新增本地运行态工具模块，封装 localStorage key、默认值、读取、写入和安全 JSON 解析
- [x] 1.3 实现运行态字段合并与损坏数据回退，确保缺失字段不会导致工作台渲染失败
- [x] 1.4 实现推进一日、切换暂停状态、追加事件和更新章节记录的纯前端辅助函数

## 2. 工作台数据接入

- [x] 2.1 调整工作台数据组合逻辑，使当前时间、运行天数、暂停状态、近期事件和最新章节优先读取 `WorldRuntimeState`
- [x] 2.2 保留无本地运行记录时的现有 mock 默认展示，避免空白模块或报错
- [x] 2.3 确认本地角色、势力、地点和关系种子仍只影响角色、关系和统计展示，不伪造运行态模块
- [x] 2.4 更新事件统计或近期事件展示逻辑，使本地事件存在时展示最新事件优先

## 3. 工作台交互实现

- [x] 3.1 将“暂停运行”按钮改为可切换暂停/继续状态，并立即更新本地运行记录和界面文案
- [x] 3.2 新增“推进一日”轻量入口，更新当前世界时间和运行天数并持久化
- [x] 3.3 新增“记录事件”轻量入口，校验必要字段后保存事件并刷新近期事件列表
- [x] 3.4 接入章节草稿/最新章节读取逻辑，确保无章节记录时使用默认章节回退
- [x] 3.5 确认所有运行态交互不调用后端接口、不写数据库、不触发 Agent 流程

## 4. 验证与回归

- [x] 4.1 运行项目静态检查或类型检查，修复 TypeScript 和 lint 问题
- [x] 4.2 在浏览器中验证首次访问、刷新恢复、暂停/继续、推进一日、记录事件和损坏本地记录回退
- [x] 4.3 验证桌面和窄屏工作台截图，确保新增入口、按钮文案和长文本不会明显溢出或重叠
- [x] 4.4 回归创建世界、本地内容种子、角色卡、关系条目和统计概览展示
