## 1. 数据模型与本地存储

- [x] 1.1 新增 `src/types/world-seed-assets.ts`，定义角色、势力、地点、关系种子和整体 `WorldSeedAssets` payload 类型
- [x] 1.2 新增 `src/lib/world-seed-assets.ts`，实现版本化 storage key、默认空状态、字段校验和损坏数据回退
- [x] 1.3 实现内容种子的读取、保存、订阅和跨标签页 `storage` 事件处理，并触发同页自定义变更事件
- [x] 1.4 实现本地种子到工作台统计、角色卡和紧张关系条目的派生函数

## 2. 工作台数据联动

- [x] 2.1 调整 `StatsAndRuntime`，让统计概览接收派生统计数据并保留事件、秘密默认展示
- [x] 2.2 调整 `CharactersPanel`，让活跃角色优先展示本地角色种子并使用默认头像占位
- [x] 2.3 调整 `RelationshipsPanel`，让紧张关系优先展示本地关系种子并处理紧张度边界
- [x] 2.4 确保运行状态、近期事件、最新故事章节、待解秘密和近期趋势不因内容种子保存而被改写

## 3. 内容种子编辑界面

- [x] 3.1 在工作台中增加内容种子入口，并复用当前水墨工作台的按钮、overlay 和面板视觉模式
- [x] 3.2 实现角色种子新增、编辑、删除和保存，覆盖姓名、身份、目标、状态字段
- [x] 3.3 实现势力种子新增、编辑、删除和保存，覆盖名称、立场、资源、冲突点字段
- [x] 3.4 实现地点种子新增、编辑、删除和保存，覆盖名称、类型、重要性字段
- [x] 3.5 实现关系种子新增、编辑、删除和保存，覆盖 A、B、关系描述、紧张度、备注字段
- [x] 3.6 为无本地种子、空列表、长文本和窄屏布局提供可读展示与稳定交互

## 4. 样式与可访问性

- [x] 4.1 在 `src/app/globals.css` 中补齐内容种子面板、分区、列表行、字段组和操作按钮样式
- [x] 4.2 确保桌面端字段、列表、按钮和文本不明显重叠，长文本不撑破容器
- [x] 4.3 确保窄屏端内容种子面板可滚动，四类种子和保存操作均可访问
- [x] 4.4 确保用户输入文本由 React 默认转义渲染，不使用 `dangerouslySetInnerHTML`

## 5. 验证

- [x] 5.1 运行 TypeScript/lint 静态检查，确认无类型和 lint 错误
- [x] 5.2 新增或更新 Playwright 验收脚本，覆盖新增种子、保存、刷新恢复和工作台联动展示
- [x] 5.3 使用桌面和窄屏视口截图检查工作台与内容种子面板，确认主要模块非空且无明显遮挡
- [x] 5.4 手动验证本地种子数据损坏时工作台回退到默认展示且页面不报错
