## 1. 类型与本地状态

- [x] 1.1 新增或扩展世界设定类型，覆盖世界名称、类型、世界背景、世界规则、风格偏好、禁止事项和核心矛盾
- [x] 1.2 新增本地设定存储 helper，包含版本化 storage key、读取、解析、保存和损坏数据回退逻辑
- [x] 1.3 实现同页订阅通知机制，确保保存设定后 dashboard 无需刷新即可更新
- [x] 1.4 实现创建表单数据到世界设定数据的映射，并兼容旧版创建临时状态

## 2. 创建流程兼容

- [x] 2.1 更新创建世界提交逻辑，让创建成功后初始化可编辑的本地世界设定
- [x] 2.2 保留现有创建页必填校验、类型选择、自定义类型和创建后跳转行为
- [x] 2.3 确认没有新增后端调用、数据库写入或 Agent 运行触发

## 3. 世界设定编辑界面

- [x] 3.1 在工作台中接入“世界设定”按钮，点击后打开当前世界设定编辑界面
- [x] 3.2 实现编辑界面字段：世界背景、世界规则、风格偏好、禁止事项、核心矛盾
- [x] 3.3 使用当前本地设定或默认世界信息填充编辑界面，避免空状态报错
- [x] 3.4 实现取消、保存和保存后关闭或反馈状态
- [x] 3.5 增加长文本输入、字段说明和空值展示处理，避免长文本撑破布局

## 4. 工作台展示联动

- [x] 4.1 更新 dashboard 数据读取逻辑，优先使用本地世界设定，缺失时回退到默认 mock 数据
- [x] 4.2 保存世界背景或风格偏好后，立即更新顶部横幅描述和标签
- [x] 4.3 确保世界规则、禁止事项和核心矛盾不会伪造或重写运行状态、事件、角色关系、秘密或趋势模块

## 5. 样式与响应式

- [x] 5.1 为设定编辑界面增加水墨主题样式，保持与现有工作台视觉一致
- [x] 5.2 验证桌面视口下字段、按钮和主要文本不重叠
- [x] 5.3 验证窄屏视口下编辑界面可滚动，所有字段和保存操作可访问

## 6. 验证

- [x] 6.1 运行 `npm run lint`
- [x] 6.2 运行 `npm run typecheck`
- [x] 6.3 使用浏览器手动或 Playwright 验证打开编辑界面、保存、刷新恢复和损坏数据回退核心路径
