## 1. 章节数据与路由

- [x] 1.1 新增类型化 mock 故事章节数据，包含 `id`、章节标题、生成时间、世界时间、主要角色、摘要和正文占位
- [x] 1.2 新增故事章节列表页路由 `src/app/stories/page.tsx`，并接入章节列表组件
- [x] 1.3 新增章节阅读路由 `src/app/stories/[chapterId]/page.tsx` 或等价详情视图，支持无效章节 ID 的可读回退

## 2. 章节列表界面

- [x] 2.1 实现故事章节列表页头部，展示页面标题、章节数量和 mock 内容状态
- [x] 2.2 实现章节列表条目，展示章节标题、生成时间、对应世界时间、主要角色、摘要和阅读按钮
- [x] 2.3 为章节字段缺失、无章节数据和长文本提供可读回退，避免空白页、溢出或遮挡

## 3. 阅读入口与导航

- [x] 3.1 让每个阅读按钮导航到对应章节阅读内容，并保留章节标题、世界时间和主要角色信息
- [x] 3.2 在阅读视图中提供返回故事章节列表页和回到工作台的入口
- [x] 3.3 如工作台已有故事章节或继续阅读入口，将其连接到 `/stories` 或对应 mock 章节阅读页，并保持现有工作台展示不回归

## 4. 样式与响应式

- [x] 4.1 为故事章节列表页和阅读视图补充水墨工作台风格样式，复用宣纸底、墨色、细线边框和低饱和视觉语言
- [x] 4.2 调整桌面端布局，确保章节元信息、摘要和阅读按钮清晰可扫描
- [x] 4.3 调整窄屏端布局，确保章节列表单列可滚动、按钮可点击、长文本不横向溢出

## 5. 验证

- [x] 5.1 运行 `npm run lint` 和 `npm run typecheck`，确认故事章节页面无静态错误
- [x] 5.2 使用浏览器验证 `/stories` 桌面和窄屏渲染，确认页面非空、章节字段完整、阅读按钮可达
- [x] 5.3 验证章节阅读页或详情视图、无效章节回退和返回列表入口均符合 spec
- [x] 5.4 确认访问和阅读 mock 章节不会调用后端、触发 Agent 或写入本地世界运行记录
