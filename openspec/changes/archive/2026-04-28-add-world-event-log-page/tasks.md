## 1. 事件数据契约

- [x] 1.1 扩展 `src/types/world-runtime.ts`，新增事件重要性类型，并为 `WorldRuntimeEvent` 增加参与角色、地点、事件影响、事件详情和重要性字段
- [x] 1.2 更新 `src/lib/world-runtime.ts` 的事件解析和规范化逻辑，确保旧事件缺少新增字段时仍可解析并获得默认值
- [x] 1.3 更新事件创建逻辑，让标题和摘要继续作为必填项，可选保存参与角色、地点、影响、详情、类型和重要性
- [x] 1.4 复用或调整事件倒序 helper，保证工作台和事件日志页面都以最新创建或最新发生的事件优先展示

## 2. 事件日志页面

- [x] 2.1 新增事件日志页面路由，例如 `src/app/events/page.tsx`，读取 active world 和对应 `worldId` 的本地运行记录
- [x] 2.2 实现事件日志主界面，展示当前世界名称、事件数量、事件列表和事件详情区域
- [x] 2.3 在事件列表项中展示时间、标题、参与角色、地点、事件影响、摘要、类型和重要性，并处理旧事件字段缺失回退
- [x] 2.4 实现事件详情选择逻辑，默认选中最新事件，用户选择其他事件后刷新详情内容
- [x] 2.5 实现无事件、无 active world 或本地运行记录损坏时的可读空状态

## 3. 工作台集成与记录入口

- [x] 3.1 将工作台近期事件模块的“查看全部”连接到事件日志页面
- [x] 3.2 将侧栏“事件”导航项或等价事件入口连接到事件日志页面，并保持其他导航项现有展示不回归
- [x] 3.3 更新工作台轻量记录事件入口，使其在新增字段缺省时仍能创建兼容的结构化事件
- [x] 3.4 确认 active world 切换后，工作台近期事件和事件日志页面都读取切换后的 `worldId` 事件

## 4. 样式与响应式

- [x] 4.1 为事件日志页面补充水墨工作台风格样式，保持宣纸底、细线边框、低饱和色和清晰信息密度
- [x] 4.2 调整桌面端列表加详情布局，确保长标题、长影响和长详情不会撑破容器
- [x] 4.3 调整窄屏端布局，确保事件列表和详情均可滚动访问且主要文本不互相遮挡

## 5. 验证与文档

- [x] 5.1 运行 TypeScript 或 lint 检查，确认新增类型、页面和组件无静态错误
- [x] 5.2 使用浏览器自动化验证 `/events` 桌面和窄屏渲染，确认页面非空、入口可达、列表和详情可读
- [x] 5.3 验证旧事件数据兼容：只有标题、摘要、类型、时间和创建时间的事件仍能在工作台与事件日志页面展示
- [x] 5.4 如实现后 README 的功能说明已过期，补充事件日志页面和本地事件字段说明
