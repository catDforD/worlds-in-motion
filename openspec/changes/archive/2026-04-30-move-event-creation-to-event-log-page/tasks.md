## 1. 工作台近期事件预览

- [x] 1.1 更新 `EventsPanel` 接口，移除 `onRecordEvent` 参数、表单状态、提交处理和记录事件相关输入控件
- [x] 1.2 保留近期事件只读列表，确保只展示最新少量事件，并在无事件时显示可读空状态和进入事件日志页的入口
- [x] 1.3 清理 `InkDashboard` 中仅用于工作台记录事件的 `handleRecordEvent`、`appendWorldRuntimeEvent` 导入和相关类型依赖
- [x] 1.4 确认工作台暂停、继续运行、运行一天、近期事件读取和事件统计展示不回归

## 2. 事件日志页记录事件

- [x] 2.1 在 `WorldEventLog` 中新增记录事件模式，提供从页面头部、列表头部或空状态进入新建表单的入口
- [x] 2.2 在事件日志页实现记录事件表单，覆盖标题、摘要、类型、重要性、参与角色、地点、影响和详情字段
- [x] 2.3 复用 `appendWorldRuntimeEvent`、`saveWorldRuntimeState` 和 active `worldId`，保存事件到当前世界本地运行记录
- [x] 2.4 实现必要字段校验、无 active world 禁止创建、取消返回详情或空状态的交互
- [x] 2.5 保存成功后刷新事件列表，确保最新事件优先，并默认展示或选中新创建事件详情

## 3. 布局与样式

- [x] 3.1 调整工作台近期事件样式，移除或收敛工作台事件表单样式，确保预览模块高度稳定
- [x] 3.2 为事件日志页新建表单补充水墨风格样式，并与现有列表、详情面板视觉语言一致
- [x] 3.3 设置事件列表和详情/新建区域的内部滚动窗口，避免大量事件或长表单撑长页面
- [x] 3.4 调整窄屏响应式布局，确保列表、详情、新建表单、按钮和校验文案均可访问且不横向溢出

## 4. 验证

- [x] 4.1 运行类型检查或 lint，确认移除工作台表单后没有未使用导入、未使用状态或类型错误
- [x] 4.2 使用浏览器自动化验证工作台桌面和窄屏截图，确认近期事件模块不再显示记录事件表单
- [x] 4.3 使用浏览器自动化验证事件日志页桌面和窄屏截图，确认可以打开记录事件模式并完整访问表单
- [x] 4.4 验证事件创建流程：空事件被拒绝，合法事件保存后进入当前 active world 的事件列表并展示为最新事件
- [x] 4.5 验证大量事件场景，确认事件日志列表内部滚动且详情或新建表单仍可访问
- [x] 4.6 运行 OpenSpec 状态或校验命令，确认 proposal、design、specs 和 tasks 均处于可应用状态
